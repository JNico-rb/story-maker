# URL del servidor de lectura. Si no se define, el visor usa "/api",
# que en desarrollo Vite redirige al servidor local (ver vite.config.ts).
# Solo se leen variables con prefijo VITE_. Nunca pongas secretos aquí.
VITE_API_BASE_URL=/api
