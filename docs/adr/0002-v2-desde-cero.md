# V2 empieza de cero; las ramas anteriores no son referencia

La primera rama fue un primer contacto con el harness, en modo vibe coding: sin un flujo de trabajo correcto y sin ceñirse al encargo. V2 rehace el mismo proyecto de forma profesional, con el flujo docs → spec → plan → tests → código y alineado con lo que se pidió. Nada de las ramas anteriores —código, prompts ni decisiones— sirve de referencia: no pasó por las puertas de aprobación, así que reutilizarlo introduciría deriva.
