# IDE
*.code-workspace
.vscode/

# API keys and secrets
.env
settings.local.json

# Clon de trabajo que usa la skill react-expert
.claude/react/

# Dependencias, builds y cachés: se regeneran, nunca se versionan
node_modules/
dist/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
.playwright-mcp/
lean/.lake/
# Directorio de datos por defecto, sin STORY_MAKER_DATA_DIR (architecture.md §15.5)
/backend/data/
tla/states/
tla/*_TTrace_*
tla2tools.jar
*.out

# Bases SQLite fuera de backend/data (p. ej. STORY_MAKER_DATA_DIR apuntando a la raíz): nunca al repo
*.db
*.db-wal
*.db-shm
